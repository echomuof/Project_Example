<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="dubbo_provider"  />

    <!-- 使用zookeeper注册中心暴露服务地址 -->
    <dubbo:registry address="zookeeper://47.93.10.179:2181" timeout="3000" />

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol host="192.168.1.103" name="dubbo" port="20770" />

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="top.okay3r.service.HelloService" ref="helloService" />

    <!-- 接口实现类-->
    <bean id="helloService" class="top.okay3r.service.impl.HelloServiceImpl"/>

</beans>